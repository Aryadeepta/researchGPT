# Python Code Implementation Skill
## Goal
Develop clean, robust, and verifiable Python implementations, and manage environment dependencies dynamically.
## Instructions
1. Write self-contained, commented Python code to fulfill the task.
2. YOU MUST wrap all Python code in triple backticks: ```python ... ```.
3. If dependencies are missing, YOU MUST write and wrap the necessary shell commands in triple backticks: ```bash ... ``` (e.g., `pip install ...`).
4. **CRITICAL: Self-Healing**: Review the 'Previous attempt logs' in the Context. If prior attempts failed (e.g., due to missing packages, syntax errors, or timeouts), you MUST fix the underlying issue before providing new code.
5. Include an execution block (e.g., `if __name__ == "__main__":`) that prints explicit diagnostic results, logs, or test outcomes to stdout. This is critical for the Adversary agent to evaluate your work.
6. If a step requires mathematical modeling, implement the model clearly with documented assumptions.
7. Anticipate and handle edge cases (e.g., division by zero, type mismatches).
8. Prioritize readability and maintainability.
