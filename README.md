# Autonomous Research Agent

This project implements an autonomous research agent capable of taking a research field, identifying potential research topics, autonomously selecting one, and producing a full paper proposal along with a functional code prototype.

## Usage
Run the main orchestrator script with the target research field:
```bash
python3 main.py --field "your research field"
```

The output will be saved in the `results/` directory.
