name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: hostname
      - run: pwd
      - run: whoami
      - run: python3 -m unittest discover -s tests
