name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - linux
      - wsl

    steps:
      - run: hostname
      - run: pwd
      - run: whoami
      - run: docker version
