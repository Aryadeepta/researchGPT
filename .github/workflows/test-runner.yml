name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - linux
      - wsl
      - research-control

    steps:
      - run: hostname
      - run: pwd
      - run: whoami
      - run: docker version
