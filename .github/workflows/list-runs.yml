name: List Research Runs
on:
  workflow_dispatch:

jobs:
  list:
    runs-on: [self-hosted, linux, research-control, cloud]
    steps:
      - uses: actions/checkout@v4
      - name: List Runs
        run: scripts/list-runs
