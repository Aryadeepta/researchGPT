name: Research List
on:
  workflow_dispatch:
jobs:
  list:
    runs-on: [self-hosted, linux, wsl]
    steps:
      - uses: actions/checkout@v4
      - name: List Research Runs
        run: scripts/wsl list
