name: Research List
on:
  workflow_dispatch:
jobs:
  list:
    runs-on: [self-hosted, linux, wsl, research-control]
    steps:
      - uses: actions/checkout@v4
      - name: List Research Runs
        run: scripts/research-control list
